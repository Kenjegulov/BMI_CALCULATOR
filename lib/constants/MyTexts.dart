import 'package:flutter/material.dart';

class MyText {
  static const bmi = Text("BMI CALCULATOR");
  static const male = Text("MALE");
  static const female = Text("FEMALE");
  static const height = Text("HEIGHT");
  static const age = Text("AGE");
  static const calculator = Text(
    "CALCULATOR",
    style: TextStyle(fontSize: 22),
  );
  static const recalculate = Text(
    "Кайра эсепте",
    style: TextStyle(
      fontSize: 22,
      fontWeight: FontWeight.w500,
    ),
  );
}
